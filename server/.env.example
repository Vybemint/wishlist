# Server
PORT=3001

# SMTP (Gmail app password recommended)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=mintvybe@gmail.com
SMTP_PASS=your_app_password_here

# Export route secret
EXPORT_SECRET=vybemint-secret-key-2026

# Database path (defaults to ./waitlist.db)
# DB_PATH=/data/waitlist.db

# API Base URL for the frontend (leave empty for relative paths)
API_BASE=https://wishlist.vybemint.api.prod.nerdsville.net
